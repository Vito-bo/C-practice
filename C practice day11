#include <stdio.h>
int main()
{
    int n;
    long long ret = 0, tmp;
    scanf("%d", &n);
    tmp = n;
    printf("%I64d", (1+tmp) * tmp / 2);
    return 0;
}


/*
int main()
{
    int r;
    double s, PI;
    scanf("%d", &r);
    PI = atan(1.0) * 4;
    s = PI * r * r;
    printf("%0.7lf", s);
    return 0;
}

int main() 
{
	int iYear;
	scanf("%d",&iYear);
	if(((iYear%4==0)&&(iYear%100!=0))||(iYear%400==0))
	{
		 printf("%d是闰年\n",iYear);
	}   
	else
	{
		printf("%d不是闰年\n",iYear);
	}
	    
	return 0;
}



#define MAXSIZE 10000 
int main()
{
	int n;
	scanf("%d",&n);
	int i=0;
	int arr[n]={0};
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	 } 
	 int max=-MAXSIZE;
	 int min=MAXSIZE;
	 int sum=0;
	 for(i=0;i<n;i++)
	 {
	 	if(arr[i]>max)
	 	{
	 		max=arr[i];
		 }
		 if(arr[i]<min)
		 {
		 	min=arr[i];
		 }
		 sum+=arr[i];
	 }
	 printf("%d\n",max);
	 printf("%d\n",min);
	 printf("%d\n",sum);
	return 0;
}

int main()
{
    int a, b;
    scanf("%d%d", &a, &b);
    printf("%d", a+b);
    return 0;
}


#define MAX 1000001
#define MOD 10007
int n, i, F[MAX];
int main()
{
	scanf("%d",&n);
	F[1]=1;
	F[2]=1;
	for (i = 3; i <= n; ++i)
        F[i] = (F[i-1] + F[i-2]) % MOD;
    printf("%d\n", F[n]);
	return 0;
}

*/





 

